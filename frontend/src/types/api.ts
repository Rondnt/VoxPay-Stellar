// Generado desde el Swagger/OpenAPI del backend con `openapi-typescript`.
// No editar a mano — placeholder hasta que exista `backend/` con endpoints reales.
//
//   npx openapi-typescript http://localhost:3001/docs-json -o src/types/api.ts

export type paths = Record<string, never>;
